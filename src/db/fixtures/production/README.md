# Production fixtures

Real, source-reviewed fighter data goes here once the data-entry and provenance
workflow is finalized (post-MVP-foundation). **Not populated during Slice 1.**

Nothing in this folder is seeded yet. Real fighter data must go through the
curated data-entry and citation workflow described in `docs/DATABASE.md`
(`source_documents` + `fighter_evidence`, publication gated by `publishFighter`)
before it lands here — it is not part of Slice 1.
